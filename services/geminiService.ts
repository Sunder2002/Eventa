/**
 * Gemini / AI integration removed.
 * Keep a stable stub so imports don't break.
 */
export const sendMessageToGemini = async (_message?: string): Promise<string> => {
  // No external calls — return an empty placeholder response.
  return '';
};
